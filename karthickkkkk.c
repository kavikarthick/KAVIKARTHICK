#include<stdio.h>
void main()
{
    int a,b,c;
    scanf(" %d %d %d",&a,&b,&c);
    if(a>b && a>c)
    {
    printf("a is  largest %d",a);
    }
    if else(b>c)
    {
    printf(" b is largest %d",b);
    }
    else
    {
    printf(" c is larest %d",c);
    }
    
}
